# OPC-UA-Wrapper-Server

Implements a OPC-UA-Server that builds its structure from provided json schemas and queries a datasource

# Performance
Test was done with 20000 values/second without problems